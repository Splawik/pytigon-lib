# schhtml – HTML Generation and Document Rendering

HTML parsing, rendering, and multi-format document output (PDF, DOCX, XLSX, ODS, Cairo graphics).

::: pytigon_lib.schhtml
    options:
      show_submodules: true
      members: true
