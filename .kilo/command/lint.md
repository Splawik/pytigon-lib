---
description: Run ruff linting
agent: code
---
Run `ruff check .` and `ruff format --check .` in the project root. Report any issues found. If there are formatting issues, ask the user before auto-fixing with `ruff format .`.
